#prepare envioronment:
1.sudo vi /etc/sudoers, add `yoursystemname  ALL=(ALL) NOPASSWD:ALL` at the end of file(if yoursystemname is deepin, then add `deepin  ALL=(ALL) NOPASSWD:ALL`)
2.need Access-token in [https://rr.deepin.io/](https://rr.deepin.io/)
3.If you want to check task [https://rr.deepin.io/#/review/303](https://rr.deepin.io/#/review/303), just replace 303 in ** rpa.info **: ` id = 303 `
4.sudo apt-get install python3-pyatspi python3-pip python3-setuptools libwnck-3-dev  python3-tk



#run script:
1.tar zxvf tar zxvf check_pkgs.tar.gz
2.cd rr
3.python3 check_pkgs.py


#check result
会生成result.html文件方便查看版本，安装，打开，删除状态
pkgs.info会输出具体的失败信息
